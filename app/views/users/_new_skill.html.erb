<div class="skill">
    <%= select "category", "category_id", Category.all.collect {|p| [ p.name, p.id ] }, :length => '20', :prompt => 'Select Category' %>
    <%= s.collection_select :id, {}, :id, :name, :include_blank => true, :rows => 10, :length => '20', :prompt => 'Select Skill' %>
    <%= link_to "Remove Skill", "#", :class => 'remove' %>
    <br />
</div>