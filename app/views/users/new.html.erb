<h1>Register</h1>

<% form_for @user, :url => account_path do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <% if params[:type] == 'employer' %>
      <% f.fields_for :employer do |e| %>
         <%= render :partial => 'new_employer', :e => e %>
      <% end %>
  <% else %>
      <% f.fields_for :worker do |builder| %>
         <%= render 'new_worker', :w => builder %>
      <% end %>
  <% end %>
  <%= f.submit "Unirse" %>
<% end %>